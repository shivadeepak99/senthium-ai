# ============================================
# SENTHIUM AI SECURITY SYSTEM - REQUIREMENTS
# ============================================

# Core System Dependencies
psutil                 # System and process utilities (CPU, memory, disk monitoring)
pyyaml                 # YAML parser for config files
jsonschema             # JSON Schema validation for configs
setuptools             # Python package development

# AI Vision & Security 🤖🔒
opencv-python          # Computer vision library (face detection via DNN)
pillow                 # Image processing (PIL)
numpy                  # Array operations for face encodings
deepface               # Face recognition with multiple models (Facenet, VGG-Face, ArcFace)
tensorflow             # Deep learning framework (required by DeepFace)
tf-keras               # Keras API for TensorFlow

# GUI Dashboard
streamlit              # Web dashboard interface
altair<5               # IMPORTANT: Streamlit breaks with altair 5+

# Alerts & Notifications 📢
discord-webhook        # Discord webhook alerts
requests               # HTTP requests for webhooks/APIs
playsound==1.2.2       # IMPORTANT: v1.3.0+ is broken, must use 1.2.2

# Cross-platform Notifications
plyer; platform_system!='Windows'  # Cross-platform notifications (Linux/macOS)

# Testing Framework (optional)
pytest                 # Testing framework
pytest-cov             # Coverage plugin for pytest
pytest-mock            # Mocking plugin for pytest

# Development Tools (optional)
black                  # Code formatter
flake8                 # Linter
mypy                   # Type checker

# Logging
colorlog               # Colored logging output

# ============================================
# WINDOWS-SPECIFIC DEPENDENCIES
# ============================================
# Only installed on Windows systems

pywin32; platform_system=='Windows'              # Windows API (Named Pipes, Services, Lock Screen)
win10toast-click; platform_system=='Windows'     # Windows 10/11 toast notifications

# ============================================
# LINUX-SPECIFIC DEPENDENCIES (Future)
# ============================================
# dbus-python; platform_system=='Linux'  # For systemd integration
